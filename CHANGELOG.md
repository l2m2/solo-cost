# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## Unreleased

### Added
- 客户成为独立实体：新增「客户」菜单页，可维护客户名、联系人、联系方式、税号、开票抬头、备注；同一公司内客户名不重复
- 项目里的客户从自由文本改为下拉必选，附「新建客户」快捷弹窗，选完立即回填；未来可按客户维度做汇总统计
- 任务列表以紧凑表格展示，新增「实际工时」列（该任务下所有未软删工时之和），超过 20 条自动分页
- 任务状态徽章按状态上色（待办灰、进行中琥珀、已完成绿），一眼可辨
- 任务新增「已关闭」状态：禅道中「已关闭且已完成」的任务导入为已关闭，与「已完成」区分；任务列表默认隐藏已关闭任务，可在状态筛选切到「已关闭」或「全部」查看
- 任务保留创建日期、实际开始、实际完成三个日期：禅道导入时按真实日期填充，任务表格新增这三列展示；新建/编辑任务表单也可手动编辑实际开始与实际完成日期
- 仪表盘上线：公司级「生意一览」，分总览 / 分布&排行 / 应收三个 Tab
  - 钱大盘两套口径：合同口径（合同总额/不含税收入/潜在到手）与已收口径（已收/未收应收/已收到手）
  - 「到手」按个人开发者视角 = 不含税收入 − 销售提成 − 非人力成本（人力成本视作个人收入不扣）
  - 按年到手：按到账年份统计每年到手，叠加当年已收（不含税）做对照
  - 项目状态分布、按到手的客户 Top5 与项目 Top5
  - 应收提醒：未到账收款节点按逾期（红）/近 30 天（黄）/未来分档，顶部显示合计未收
  - 按年到手可点击查看当年到手明细：弹窗按项目拆解到手组成（实收/成本/提成）+ 按到账日列出当年每一笔收款（项目·节点名·含税金额）

### Changed
- 任务操作从状态下拉框改成行内图标按钮：待办点「开始」进入进行中、非已完成点「完成」直接结束，另有工时/编辑/删除
- 删除项目改为二次确认：弹窗中需输入项目代号完全一致才可删除，防止误删
- 禅道 CSV 导入改为按任务编号升序导入，任务顺序与禅道编号保持一致
- 任务表格默认不再显示「截止日期」列（仍可在编辑表单中维护）
- 「按模块统计人力成本」卡片默认折叠，标题栏常驻显示合计工时与人力成本，点击展开明细，模块多时不再挤占任务表格空间
- 收款节点编辑表单现可直接修改实收金额与实收日期，清空日期即视为未收
- 仪表盘右上角新增「刷新」按钮，修改收款/成本后无需切换公司即可重载数据

### Removed
- 项目的自由文本客户字段被移除；升级后现有项目的客户会清空，需重新到项目里从下拉挑选绑定

### Fixed
	- 编辑收款节点时不再误将实收金额与实收日期清空：编辑表单原缺失这两个字段，导致保存时把已收数据覆盖为空值，现已从初始值正确回填
	- 编辑收款节点时，若目标节点不存在，现在返回「未找到」错误而非将其误判为参数校验失败
- 新建或编辑任务时，若指派的负责人不属于该项目所在公司或该成员已归档/删除，将被拒绝并给出明确提示
- 成本与收款节点的写操作现在保证即使数据刷新失败，财务汇总也会同步更新，数据面板不再出现数字滞后
- 编辑工时记录时，若「工作日期」为空则前端拦截并提示必填，不再允许提交空日期
- 删除任务的确认弹窗现在显示任务标题，并说明可在回收站恢复，提示信息更清晰
- 编辑任务时，若原负责人已被归档，下拉列表中仍会显示该成员（标注「已归档」），不再出现选中值对应空白的问题

### Added
- 禅道 CSV 导入：项目详情「任务+工时」tab 顶部新增「从禅道 CSV 导入」按钮，5 步 wizard 完成成员/模块映射与幂等导入（`tasks.external_ref = "zentao:<编号>"` 命中则整行跳过），支持 UTF-8 与 GBK 编码
- 项目模块：每个项目可创建扁平模块列表，任务可选挂到某个模块（默认未分类），项目详情「任务+工时」tab 顶部支持按模块筛选与「按模块统计人力成本」卡；模块删除时若有任务在挂被强拒绝
- 项目支持销售提成配置：`不算` / `按已回款含税额 × 提成率` / `固定金额 + 手工入账` 三种模式，后端自动计入 `total_cost`，项目详情财务面板独立展示「销售提成」卡
- 设置页（`/settings`）正式上线：展示备份状态卡（上次备份时间、已保留份数），支持「立即备份」、「导出明文备份」及历史备份列表查看
- 数据库损坏保护：解锁时检测到完整性异常，启动自动阻塞在「数据库损坏」对话框，列出可用自动备份供用户直接选择，或手动浏览选择备份文件后输入主密码一键恢复；恢复成功后自动切回锁定页面等待重新登录
- 解锁后自动判定是否需要 24 小时周期备份，满足条件时后台静默快照，不影响前台操作
- 新增「从备份还原」功能：覆盖当前数据库前，先用给定密码试打开备份文件并执行完整性检查，双重校验通过后才关闭现有连接、替换数据库文件并重新打开；前端触发还原后需重新走锁定/解锁流程以重置所有数据缓存
- 解锁（unlock）现在在迁移成功后额外执行 PRAGMA integrity_check；检测到数据损坏时返回 IntegrityCheckFailed 错误，前端可据此弹出恢复对话框引导用户选择备份恢复
- 备份 Tauri 命令层：新增 5 条命令供前端调用——立即备份（create_backup_now）、备份列表查询（list_backups）、24 小时自动备份检测（maybe_run_auto_backup）、明文导出到指定路径（export_plaintext_backup）、备份状态查询（get_backup_status，返回上次备份时间、备份数量、是否到期）；自动备份文件命名格式 auto_YYYYMMDD_HHmmss.db，rotation 保留最近 7 份
- 备份基础能力：支持 WAL checkpoint、加密数据库文件复制、自动备份列表查询（仅 auto_* 命名）、超出上限时保留最新 7 份并删除旧备份、明文导出（通过 SQLCipher 内置 `sqlcipher_export`）、完整性检查（PRAGMA integrity_check）、24 小时自动备份判定
- 新增错误类型：完整性检查失败（IntegrityCheckFailed）与备份操作失败（Backup），错误信息携带具体原因
- 项目详情「概览」Tab 升级为完整财务面板：展示含税/不含税收入、税额、一般成本、人力成本、总成本、毛利润与利润率、回款率（含实收/预期金额）共 11 项财务指标，以及客户、起止日期、备注基本信息；数据来源于财务 store，加载中显示"—"
- 成本录入的新增、编辑、删除操作完成后自动刷新财务汇总，财务面板数字实时联动
- 切换当前公司时，若正在查看其他公司的项目详情，自动跳回项目列表，防止数据错乱（M2 final review I-2 修复）
- 项目详情新增「任务+工时」Tab：任务列表支持按状态筛选（全部 / 待处理 / 进行中 / 已完成）、新建、编辑、删除及行内切换状态；每条任务可展开独立工时弹窗，录入/编辑/删除工时记录；工时列每行展示当条人力成本（工时 / 8 × 写入时日成本快照，调薪后历史不变）；删除任务时级联删除关联工时并自动刷新财务汇总
- 项目详情新增「收款」Tab：展示预期合计、实收合计、回款率三个统计卡片；节点列表支持新建、编辑、删除；「标实收」按钮仅对未到账节点显示，点击后填写实收金额与到账日期；所有写操作后自动刷新财务汇总
- 成员管理页（`/members`）：列出当前公司全部成员，在职成员排在前、已归档成员半透明置于末尾；支持新建、编辑、归档/取消归档切换；删除前弹窗确认，若成员存在工时记录则后端拒绝并以 toast 提示改用归档
- 新增成员、收款节点、任务、工时、财务汇总五类前端数据类型，供后续 UI 页面直接引用
- 新增工时输入组件（HoursInput），支持 0–24 小时范围、0.25 精度步进
- 新增 i18n 命名空间：成员、收款节点、任务、任务状态、工时记录、财务面板，中文文案已就绪
- 新增财务 store，按项目缓存财务汇总数据，拉取失败静默处理（展示"—"）
- shadcn Checkbox 组件集成
- 锁定操作（lock）现自动清空所有实体 store 缓存，再次解锁后各页面可拉取最新数据（修复 M2 遗留问题 I-1）
- 回收站现支持列出、恢复、物理清除已删任务、合同收款节点、工时记录；物理清除任务时级联清除其工时记录
- 新增 `get_project_financial_summary` 命令，前端可直接获取项目完整财务汇总（合同收入、税额、人力成本、毛利润、回款进度等）
- 工时记录后端：支持按任务或项目查询、创建、编辑、软删除；创建时自动将成员当前日成本（`daily_cost_cents`）写入快照字段，调薪后历史工时成本不受影响；编辑仅允许修改工时数、工作日期与备注；创建时校验任务与成员属于同一公司，跨公司写入被拒绝
- 任务管理后端：支持创建、查询、编辑、按状态筛选（todo / in_progress / done）及软删除；删除任务时级联软删该任务的所有工时记录
- 合同收款节点后端：支持创建、查询、编辑、软删除，新节点按添加顺序排列；`mark_payment_received` 专用命令一键记录实收金额与到账日期
- 成员管理后端：支持创建、查询、编辑、归档切换（`set_member_active`）及软删除；成员存在有效工时记录时拒绝删除并返回 `DeleteBlocked`
- 项目财务汇总：支持计算含税/不含税合同收入换算、税额拆分、人力成本（逐条舍入后累加，避免浮点漂移）、综合成本、毛利润及利润率、回款进度与回款率，分母为零时安全返回 0
- 项目软删除现级联覆盖收款节点、任务、工时记录（整组同一时间戳），恢复项目时整棵子树一并恢复；任务软删除同步软删其下工时记录，恢复任务若父项目已删则阻断并提示先恢复项目；成员若存在有效工时记录则拒绝软删除
- 数据库迁移 0003：新增成员（members）、合同收款（contract_payments）、任务（tasks）、工时记录（time_logs）四张业务表，支持成员管理、收款节点跟踪、任务分配、工时记录；Schema version 2 → 3
- 工时与日成本快照约束：time_logs 表约束 hours 取值 0-24、daily_cost_snapshot_cents 非负，确保数据合理性
- 集成 Tauri 2，支持将前端打包为原生桌面应用
- 统一错误类型 `AppError`，可序列化传递给前端展示
- 应用状态容器，持有加密数据库连接
- `ping` IPC 命令，前端可验证与 Rust 后端的通信通路
- 前端首页展示 IPC 响应（`ipc: pong`），确认打通前后端
- 主密码命令：`setup`（初始化加密数据库）、`unlock`（密码验证并解锁）、`lock`（锁定）、`is_initialized`（检查是否已初始化）
- 公司管理页面：支持创建、编辑公司信息（工商名、税号、默认税率等），并可切换当前公司
- Header 顶部公司切换器：通过下拉菜单快速切换当前公司，支持锁定操作
- 仪表盘显示当前所选公司名称，无公司时引导用户前往创建
- 项目、成本科目、成本录入三张表（schema_version 2）
- `AppError::DeleteBlocked` 变体，用于拒绝删除语义
- `domain::soft_delete` 模块：项目与成本录入的软删除/恢复，支持同一时间戳级联删除及按时间戳整组恢复（恢复项目时不影响独立删除的成本条目）；恢复成本条目时若关联项目已删则阻断并提示先恢复项目
- 成本科目管理后端：支持创建、重命名、软删除、列表查询
- 首次访问公司时自动幂等种子 9 个系统预设科目（外包成本、硬件采购、服务器与SaaS 等），预设科目不可删除
- 删除系统预设科目或仍被成本录入引用的科目时返回 `DeleteBlocked` 错误
- 项目管理后端：支持创建、查询、编辑、按状态筛选（洽谈中/待启动/进行中/已交付/已结算/已归档）、状态切换及软删除
- 删除项目时级联软删除其下所有成本录入
- 新建项目时自动为所属公司幂等种入 9 个预设科目，确保成本录入始终有科目可选
- 成本录入管理后端：支持按项目创建、编辑、软删除、列表查询；创建时校验金额非负、日期必填，并拒绝跨公司科目引用
- 项目维度科目汇总接口 `get_project_cost_summary`：按科目聚合金额，仅含有支出的科目，按金额降序排列
- 回收站后端：按当前公司列出软删除的项目与成本录入，整组恢复或物理清理（`list_trash` / `restore_trash_item` / `purge_trash_item`）
- 物理清理项目时先级联删除其下成本录入，避免外键残留
- 金额工具：人民币分↔元互转，以及带千位分隔符的 ¥ 格式化展示
- 项目状态映射工具：6 种状态的中文标签与 Badge 样式色彩
- MoneyInput 受控输入组件：前缀 ¥ 符号，输入时内部维护文本状态，失焦前不传播非法值
- 前端 TS 类型定义扩展：新增项目、成本科目、成本录入、科目汇总、回收站条目等 9 个接口
- UI 组件库扩充：新增 Tabs、Select、Badge、Table、Textarea 五个基础组件
- 前端多语言扩展：新增成本科目、项目、成本录入、回收站、状态映射的中文文案
- 成本科目管理页面：列出当前公司全部科目，预设科目以 Badge 标注只读，自定义科目支持重命名和删除；切换公司时自动重新加载并触发预设初始化
- 侧边栏新增"项目"、"成本科目"、"回收站"三个导航入口，完善主导航结构
- 项目列表页面：支持新建、编辑、软删除项目，以及按状态筛选；卡片展示合同金额、含税/不含税标识、税率及 6 种状态徽章
- 项目详情页：5-Tab 框架（概览 / 成本 / 收款（M3）/ 任务+工时（M3）/ 附件（M4））；顶部可直接切换项目状态
- 概览 Tab：以卡片形式展示合同总价（含税/税率）、客户、起止日期及备注
- 成本 Tab：成本合计、按科目汇总、成本明细表格，支持新增、编辑、删除成本录入
- 回收站页面：按当前公司列出已软删项目与成本，可恢复原位或彻底删除（彻底删除需二次确认）

### Fixed
- 编辑成本录入时补充跨公司科目校验：所选科目必须归属于该项目的公司，与新建时的校验逻辑对齐，消除 M2 遗留安全缺口

### Milestone
- **M4 里程碑完工**：数据完整性 + 备份 + 恢复。unlock 后跑 `PRAGMA integrity_check`；启动/手动/明文三类备份；`restore_from_backup` 前先用密码 + integrity 双重验证；数据库损坏时启动阻塞在恢复对话框。同时收尾 M3 遗留 minor：cross-project 归属 / assignee 公司校验 / financial refresh try/finally 保护 / TimeLogEditForm 日期校验 / task 删除 i18n / TaskForm 归档 assignee 兼容。
- **M3 完工**：成员管理（含归档/有工时则拒删）+ 合同收款节点（预期/实收/回款率）+ 任务（todo/in_progress/done）+ 工时录入（写时锁定日成本快照）+ 完整财务面板（含税/不含税收入、税额、一般+人力成本、毛利润、利润率、回款率）。同时修复 M2 final review I-1（lock 时所有 store reset）与 I-2（切换公司时跳回项目列表）。
- **M2 完工**：项目 CRUD + 6 状态生命周期 + 成本科目（9 预设 + 自定义）+ 成本录入（按项目 & 科目录入，含按科目汇总）+ 软删除回收站（级联软删、整组恢复、彻底清理）；多公司数据完全隔离
